@model ReviewIT_FinalProject_IMDBSYS2.Models.Question

@{
    ViewData["Title"] = "Create";
}

<h1 class="fw-bold text-primary text-center pb-2">Creating a Question</h1>

<div class="row border border-primary border-2 rounded-3 p-3 shadow">
    <div class="col-md-4">
        <form asp-action="Create" asp-route-quizId="@ViewBag.QuizId">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="QuestionText" class="control-label fw-bold fs-4 text-primary">Enter your Question here</label>
                <input asp-for="QuestionText" class="form-control mt-1" />
                <span asp-validation-for="QuestionText" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="QuestionTypeId" class="control-label fw-bold fs-4 text-primary">Choose a Category</label>
                <select asp-for="QuestionTypeId" class="form-control mt-1" asp-items="ViewBag.QuestionTypeId"></select>
                <span asp-validation-for="QuestionTypeId" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <input type="submit" value="Create" class="btn btn-primary fw-bold" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-route-quizId="@ViewBag.QuizId" class="btn btn-secondary fw-bold m-2">
        <i class="fa-solid fa-angle-left text-light"></i> Back to List
    </a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
